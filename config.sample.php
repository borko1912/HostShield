<?php
// HostShield: location of the data folder (backups, logs, settings).
// The installer creates config.php for you. Copy this file only for a manual setup.
// The folder must be OUTSIDE the web root, so nobody can download backups.
return ['data_dir' => '/home/USER/hostshield-data'];
